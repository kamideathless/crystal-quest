# 🎮 Crystal Quest - Установка по файлам

## 📥 ВЫ СКАЧАЛИ ВСЕ ФАЙЛЫ? 

Отлично! Теперь разложите их по правильным папкам.

---

## 📂 Шаг 1: Создайте структуру проекта

Создайте папку `crystal-quest-base` и внутри неё создайте такую структуру:

```
crystal-quest-base/
├── app/
│   ├── .well-known/
│   │   └── farcaster.json/
│   │       └── (сюда пойдет route.ts)
│   ├── (сюда пойдут layout.tsx, page.tsx, globals.css)
├── components/
│   └── (сюда пойдут Game.tsx, Game.module.css)
├── public/
│   └── (сюда будут изображения)
└── (остальные файлы в корне)
```

**Команды для создания папок:**

### Windows (PowerShell):
```powershell
mkdir crystal-quest-base
cd crystal-quest-base
mkdir app
mkdir app\.well-known
mkdir app\.well-known\farcaster.json
mkdir components
mkdir public
```

### macOS/Linux (Terminal):
```bash
mkdir -p crystal-quest-base/app/.well-known/farcaster.json
mkdir -p crystal-quest-base/components
mkdir -p crystal-quest-base/public
cd crystal-quest-base
```

---

## 📝 Шаг 2: Разложите файлы

### В корень проекта `crystal-quest-base/`:
- ✅ `package.json`
- ✅ `tsconfig.json`
- ✅ `next.config.js`
- ✅ `.gitignore`
- ✅ `.env.local.example`
- ✅ `README.md`
- ✅ `QUICKSTART.md`
- ✅ `INSTALLATION_GUIDE.md`
- ✅ `DEPLOYMENT_GUIDE.md`

### В папку `app/`:
- ✅ `layout.tsx`
- ✅ `page.tsx`
- ✅ `globals.css`

### В папку `app/.well-known/farcaster.json/`:
- ✅ `route.ts`

### В папку `components/`:
- ✅ `Game.tsx`
- ✅ `Game.module.css`

### В папку `public/`:
- ℹ️ Пока пусто - изображения создадите позже

---

## 🚀 Шаг 3: Установка и запуск

Откройте терминал в папке `crystal-quest-base` и выполните:

```bash
# Установите Node.js если ещё не установлен
# Скачайте с https://nodejs.org (версия 18 или новее)

# Установите зависимости
npm install

# Запустите локально
npm run dev
```

Откройте http://localhost:3000 - игра должна работать! 🎮

---

## 📋 Список всех файлов для скачивания:

### Основные файлы TypeScript:
1. `route.ts` - манифест Mini App
2. `layout.tsx` - layout Next.js
3. `page.tsx` - главная страница
4. `Game.tsx` - компонент игры

### Стили:
5. `globals.css` - глобальные стили
6. `Game.module.css` - стили игры

### Конфигурация:
7. `package.json` - зависимости
8. `tsconfig.json` - TypeScript конфиг
9. `next.config.js` - Next.js конфиг
10. `.gitignore` - git ignore
11. `.env.local.example` - пример env

### Документация:
12. `README.md` - общая документация
13. `QUICKSTART.md` - быстрый старт
14. `INSTALLATION_GUIDE.md` - полная установка
15. `DEPLOYMENT_GUIDE.md` - деплой и награды

### Бонус (опционально):
16. `match3-base-game.html` - standalone HTML версия
17. `farcaster.json` - пример манифеста

---

## ✅ Проверка правильности установки:

После того как разложили все файлы, структура должна быть такой:

```
crystal-quest-base/
├── app/
│   ├── .well-known/
│   │   └── farcaster.json/
│   │       └── route.ts          ✓
│   ├── layout.tsx                ✓
│   ├── page.tsx                  ✓
│   └── globals.css               ✓
├── components/
│   ├── Game.tsx                  ✓
│   └── Game.module.css           ✓
├── public/
│   └── (пусто)
├── package.json                  ✓
├── tsconfig.json                 ✓
├── next.config.js                ✓
├── .gitignore                    ✓
├── .env.local.example            ✓
├── README.md                     ✓
├── QUICKSTART.md                 ✓
├── INSTALLATION_GUIDE.md         ✓
└── DEPLOYMENT_GUIDE.md           ✓
```

---

## 🎯 Что дальше?

1. **Откройте QUICKSTART.md** - там пошаговая инструкция на 10 минут
2. Или **INSTALLATION_GUIDE.md** - подробная инструкция со всеми деталями

---

## 🆘 Проблемы?

### Не можете создать папку `.well-known`?
**Windows:** Используйте PowerShell или создайте через Проводник (включите показ скрытых файлов)
**macOS/Linux:** В терминале команда выше должна работать

### Не знаете как скачать файлы по одному?
Кликните на каждый файл в интерфейсе Claude, потом кнопка "Download" или "Скачать"

### npm install не работает?
Установите Node.js с https://nodejs.org (выберите LTS версию)

---

## 📞 Готовы начать?

**Следующий шаг:** Откройте `QUICKSTART.md` и следуйте инструкциям!

🚀 Успехов с запуском вашей игры на Base!
