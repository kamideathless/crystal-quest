# 🚀 Установка и развертывание Crystal Quest

## 📁 Структура проекта Next.js

Ваш проект должен иметь следующую структуру:

```
crystal-quest-base/
├── app/
│   ├── .well-known/
│   │   └── farcaster.json/
│   │       └── route.ts          # Манифест Mini App
│   ├── layout.tsx                # Layout с метаданными
│   ├── page.tsx                  # Главная страница
│   └── globals.css               # Глобальные стили
├── components/
│   ├── Game.tsx                  # Компонент игры
│   └── Game.module.css           # Стили игры
├── public/
│   ├── icon.png                  # Иконка 512x512
│   ├── splash.png                # Splash screen 1080x1920
│   ├── hero.png                  # Hero image 1200x630
│   ├── og-image.png              # OG image 1200x630
│   ├── embed-image.png           # Embed preview
│   └── screenshot1-3.png         # Скриншоты
├── .env.local.example
├── package.json
├── tsconfig.json
└── next.config.js
```

## 🛠️ Шаг 1: Создание проекта

### Вариант А: С нуля

```bash
# Создайте новый Next.js проект
npx create-next-app@latest crystal-quest-base
# Выберите: TypeScript ✓, ESLint ✓, Tailwind CSS ✓, App Router ✓

cd crystal-quest-base
```

### Вариант Б: Используя готовые файлы

1. Создайте папку проекта:
```bash
mkdir crystal-quest-base
cd crystal-quest-base
```

2. Скопируйте все предоставленные файлы в соответствующие папки

## 📦 Шаг 2: Установка зависимостей

```bash
npm install @farcaster/miniapp-sdk
```

## 📂 Шаг 3: Создание структуры папок

```bash
# Создайте необходимые директории
mkdir -p app/.well-known/farcaster.json
mkdir -p components
mkdir -p public
```

## 📝 Шаг 4: Копирование файлов

Разместите файлы в соответствующие папки:

1. **app/.well-known/farcaster.json/route.ts**
   - Скопируйте содержимое из `route.ts`

2. **app/layout.tsx**
   - Скопируйте содержимое из `layout.tsx`

3. **app/page.tsx**
   - Скопируйте содержимое из `page.tsx`

4. **app/globals.css**
   - Скопируйте содержимое из `globals.css`

5. **components/Game.tsx**
   - Скопируйте содержимое из `Game.tsx`

6. **components/Game.module.css**
   - Скопируйте содержимое из `Game.module.css`

7. **package.json**
   - Убедитесь что у вас есть все зависимости

## 🎨 Шаг 5: Создание изображений

Создайте следующие изображения и поместите их в папку `public/`:

### icon.png (512x512px)
- Квадратная иконка приложения
- Формат: PNG с прозрачностью
- Пример: Кристалл или логотип игры

### splash.png (1080x1920px)
- Вертикальный экран загрузки
- Фон цвета #0A0E27
- Добавьте логотип и название игры

### hero.png (1200x630px)
- Горизонтальное превью для социальных сетей
- Покажите игровой процесс

### og-image.png (1200x630px)
- Open Graph изображение
- Может быть таким же как hero.png

### embed-image.png (1200x630px)
- Превью при шаринге в Farcaster
- Привлекательный скриншот игры

### screenshot1.png, screenshot2.png, screenshot3.png
- Скриншоты игрового процесса
- Рекомендуемый размер: 1080x1920px

**Совет:** Используйте Canva, Figma или AI генераторы для создания изображений

## ⚙️ Шаг 6: Настройка переменных окружения

Создайте файл `.env.local`:

```bash
cp .env.local.example .env.local
```

Пока оставьте значение по умолчанию:
```
NEXT_PUBLIC_URL=https://your-domain.vercel.app
```

## 🧪 Шаг 7: Локальное тестирование

```bash
npm run dev
```

Откройте http://localhost:3000 - игра должна работать!

## 🌐 Шаг 8: Деплой на Vercel

### Через GitHub:

1. Создайте репозиторий на GitHub:
```bash
git init
git add .
git commit -m "Initial commit: Crystal Quest"
git branch -M main
git remote add origin https://github.com/ваш-username/crystal-quest.git
git push -u origin main
```

2. Перейдите на [vercel.com](https://vercel.com)
3. Нажмите "New Project"
4. Импортируйте ваш GitHub репозиторий
5. Vercel автоматически определит Next.js
6. Нажмите "Deploy"

### Через Vercel CLI:

```bash
npm install -g vercel
vercel login
vercel
```

## 🔗 Шаг 9: Обновление URL

После деплоя:

1. Скопируйте URL (например: `crystal-quest-abc123.vercel.app`)
2. Обновите `.env.local`:
```
NEXT_PUBLIC_URL=https://crystal-quest-abc123.vercel.app
```
3. Добавьте эту переменную в Vercel:
   - Перейдите в Settings → Environment Variables
   - Добавьте `NEXT_PUBLIC_URL` со значением вашего URL
   - Redeploy проект

## 🔐 Шаг 10: Account Association

1. Перейдите на https://www.base.dev/preview?tab=account
2. Введите ваш URL (без https://, например: `crystal-quest-abc123.vercel.app`)
3. Нажмите "Submit"
4. Нажмите "Verify"
5. Следуйте инструкциям для подписи через Base Account
6. Скопируйте сгенерированные поля:
   - `header`
   - `payload`
   - `signature`

7. Обновите файл `app/.well-known/farcaster.json/route.ts`:
```typescript
"accountAssociation": {
  "header": "скопированный_header",
  "payload": "скопированный_payload",
  "signature": "скопированная_signature"
}
```

8. Сделайте commit и push:
```bash
git add .
git commit -m "Add account association"
git push
```

Vercel автоматически пересоберет проект.

## ✅ Шаг 11: Проверка

1. Перейдите на https://www.base.dev/preview
2. Введите ваш URL
3. Проверьте:
   - ✓ Превью отображается
   - ✓ Кнопка "Play Crystal Quest" работает
   - ✓ Игра запускается
   - ✓ Все изображения загружаются

4. Проверьте вкладку "Metadata":
   - ✓ Все поля манифеста заполнены
   - ✓ Account association валидирован

## 🚀 Шаг 12: Публикация в Base App

1. Откройте Base App на телефоне
2. Создайте новый пост
3. Вставьте URL вашего приложения
4. Опубликуйте!

Ваше приложение теперь доступно как Mini App в Base!

## 📊 Шаг 13: Верификация для наград

1. Перейдите на https://base.dev
2. Найдите свое приложение
3. Подайте заявку на верификацию
4. Заполните форму:
   - Описание приложения
   - Категория: Games
   - Теги: match3, puzzle, blockchain
   - Скриншоты

## 🎯 Чеклист развертывания

- [ ] Проект создан локально
- [ ] Все зависимости установлены
- [ ] Структура папок создана
- [ ] Все файлы на своих местах
- [ ] Изображения созданы и добавлены
- [ ] Локальный тест пройден
- [ ] Деплой на Vercel выполнен
- [ ] NEXT_PUBLIC_URL обновлен
- [ ] Account association создан
- [ ] Манифест обновлен
- [ ] Превью работает на base.dev
- [ ] Игра работает в Base App
- [ ] Пост создан в Base App
- [ ] Заявка на верификацию подана

## 🆘 Решение проблем

### Проблема: "Cannot find module '@farcaster/miniapp-sdk'"
```bash
npm install @farcaster/miniapp-sdk
```

### Проблема: Изображения не загружаются
- Убедитесь что все изображения в папке `public/`
- Проверьте названия файлов (должны совпадать с manifest)
- Проверьте что изображения закоммичены в git

### Проблема: Манифест не доступен
- Проверьте путь: `app/.well-known/farcaster.json/route.ts`
- Проверьте что файл route.ts экспортирует функцию GET
- Откройте в браузере: `https://your-domain.vercel.app/.well-known/farcaster.json`

### Проблема: Account association не валидируется
- Убедитесь что вы используете правильный Base Account
- Проверьте что все три поля (header, payload, signature) заполнены
- Убедитесь что URL в манифесте совпадает с URL в account association

## 📚 Полезные ссылки

- [Base Mini Apps Docs](https://docs.base.org/mini-apps)
- [Base Build](https://base.dev)
- [Vercel Docs](https://vercel.com/docs)
- [Next.js Docs](https://nextjs.org/docs)

---

**Готово! Ваша игра теперь работает в экосистеме Base! 🎮💎**
